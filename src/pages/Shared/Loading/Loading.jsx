import React from 'react';

const Loading = () => {
    return (
        <>
            <span>Loading </span>
            <span className="loading loading-bars loading-lg"></span>
        </>
    );
};

export default Loading;